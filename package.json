{
  "name": "appstore-backend",
  "version": "1.0.0",
  "private": true,
  "description": "pearOS App Store backend: GitHub Issue Forms -> reviewed PR -> static JSON API on GitHub Pages",
  "scripts": {
    "parse-issue": "node scripts/parse-issue.js",
    "build-database": "node scripts/build-database.js",
    "validate": "node scripts/validate.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
